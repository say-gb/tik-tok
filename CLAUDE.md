# CLAUDE.md

이 리포의 규약은 **`AGENTS.md` 한 곳에 모여 있다.** Codex와 공유하는 파일이다.
아래 두 줄로 불러오니, 규칙을 고칠 땐 이 파일이 아니라 그 두 파일을 고친다.

@AGENTS.md

@낭만클럽-세팅/CLAUDE.md

---

## Claude Code 전용 안내

- 슬래시 명령은 `낭만클럽-세팅/.claude/skills/` 의 스킬로 붙어 있다:
  `/스레드` `/칼럼` `/브랜드톤` `/밈트렌드` `/사업전략` `/브런치`
- 명령 없이 그냥 물어도 스킬을 알아서 꺼내 쓴다.
- 작업을 Codex에 넘길 때는 `.agents/작업일지.md`에 한 줄 남긴다. 그게 유일한 인수인계 방법이다.
- 커밋 메시지 마지막 줄에 `Agent: claude` 를 남긴다.
