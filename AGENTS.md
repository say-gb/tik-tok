# AGENTS.md — 이 리포에서 일하는 AI 에이전트 공통 규약

이 리포는 **최경집 교수(GB · 낭만클럽)의 콘텐츠·브랜드 운영 리포**다.
**Claude Code**와 **OpenAI Codex**가 같은 리포를 번갈아, 때로는 동시에 쓴다.
어느 쪽으로 들어왔든 규약은 이 파일 하나다.

- Codex는 이 `AGENTS.md`를 자동으로 읽는다.
- Claude Code는 루트 `CLAUDE.md`가 이 파일을 불러오게 돼 있다.
- 그래서 **규칙을 고칠 땐 이 파일과 `낭만클럽-세팅/CLAUDE.md`만 고치면 양쪽에 다 적용된다.**

---

## 0. 세션 시작할 때 반드시 읽는 것 (순서대로)

| 순서 | 파일 | 무엇이 들었나 |
|---|---|---|
| 1 | `AGENTS.md` (이 파일) | 협업 규약 |
| 2 | `낭만클럽-세팅/CLAUDE.md` | 교수님 정체성·말투·결과물·검증 규칙 — **단일 원본** |
| 3 | `.agents/작업일지.md` | 직전에 누가 무엇을 어디까지 했는지 |

2번은 **요약본을 믿지 말고 원본을 직접 열어 읽는다.** 말투 규칙을 어긴 결과물은 완성이 아니다.

---

## 1. 기본 역할 분담

교수님이 따로 지시하면 그 지시가 우선. 지시가 없으면 이 기본값으로 움직인다.

| | Claude Code | Codex |
|---|---|---|
| 잘하는 일 | 글·톤·스킬 기반 콘텐츠, 리서치·검증, 문서 구조 | 코드·스크립트, 렌더링/자동화, 리팩터링, 대량 파일 처리 |
| 주로 만지는 곳 | `낭만클럽-세팅/`(원고·분석·브런치), `.claude/` | `카드뉴스/*/render.py`, `.github/workflows/`, `.agents/*.py` |
| 브랜치 접두사 | `claude/` | `codex/` |

**넘길 때는 말로 넘기지 않는다.** `.agents/작업일지.md`에 한 줄 남기고 넘긴다.

---

## 2. 충돌 안 나게 하는 규칙 (이게 제일 중요)

1. **작업 시작 전에 항상 최신화한다.** `git fetch origin && git pull --rebase origin master`
2. **기본 브랜치는 `master`.** 직접 커밋하지 않고 자기 접두사 브랜치에서 작업한다.
3. **한 파일은 한 에이전트만.** 작업 시작할 때 `.agents/작업일지.md`의 `## 지금 잡고 있는 파일`에 경로를 적고, 끝나면 지운다. 거기 이미 적혀 있는 파일은 손대지 않는다. 손대야 하면 교수님께 먼저 묻는다.
4. **커밋은 작게, 자주.** 원고 하나 = 커밋 하나.
5. **커밋 메시지 마지막 줄에 누가 했는지 남긴다.**
   `Agent: claude` 또는 `Agent: codex`
6. **푸시는 `git push -u origin <브랜치명>`.** 실패하면 2초 → 4초 → 8초 → 16초 간격으로 최대 4번까지만 재시도.
7. PR은 **교수님이 만들라고 할 때만** 만든다.

---

## 3. 요청 → 어디를 읽어야 하나 (라우팅)

Claude Code는 `/스레드` 처럼 슬래시 명령으로 자동 발동한다.
Codex는 슬래시 명령이 없으므로 **아래 표를 보고 해당 `SKILL.md`를 직접 열어 읽은 다음** 그 규칙대로 쓴다.

| 이런 요청이 오면 | 먼저 읽을 파일 |
|---|---|
| 스레드·인스타 글, 아침 글, SNS 캡션 | `낭만클럽-세팅/.claude/skills/스레드/SKILL.md` |
| 칼럼·사설·기고문, 칼럼 제목 | `낭만클럽-세팅/.claude/skills/칼럼/SKILL.md` |
| 톤 점검, "내 말투로 바꿔줘" | `낭만클럽-세팅/.claude/skills/브랜드톤/SKILL.md` |
| 요즘 밈·트렌드·유행어 | `낭만클럽-세팅/.claude/skills/밈트렌드/SKILL.md` |
| 제안서·전략·사업 기획 | `낭만클럽-세팅/.claude/skills/사업전략/SKILL.md` |
| 브런치 연재 「낭만은 팔리는가」 | `낭만클럽-세팅/.claude/skills/브런치/SKILL.md` + `브런치/목차.md` + `브런치/01_*.md` |
| 카드뉴스 렌더링 | 해당 폴더의 `render.py`, `index.html` |

---

## 4. 양쪽 다 지키는 최소 규칙

- **한국어 존댓말, 호칭은 "교수님". 핵심 결론 먼저.** 서론·예열 금지.
- **결과물은 복붙용 완성본으로.** "이런 식으로 쓰시면 됩니다" 금지.
- **검증**: 최신 정보·가격·제도·기관·제품 스펙·일정·법률/교육 정보는 검색해 확인하고 답한다. 확인 못 한 건 `[미검증]` / `[추정]` 표기. 모르면 모른다고 한다.
- **지어내기 금지**: 교수님의 개인 경험·일화를 상상해서 쓰지 않는다. 학력·거주지·건강·정치 성향은 추정도 언급도 하지 않는다.
- 자세한 말투 규칙은 전부 `낭만클럽-세팅/CLAUDE.md`에 있다. 그게 원본이다.

---

## 5. 실행 환경 메모

- 파이썬은 **`python`** 으로 부른다. 교수님 PC의 `python3`은 스토어 스텁이라 조용히 죽는다.
- ffmpeg 8.1.2 / Node 24 / Pillow 12 설치됨. `sips`·`open`·`textutil`·`brew`는 없다(맥 전용).
- 루트 `.claude/skills/` 는 외부 라이선스(CC BY-ND 4.0) 스킬이라 **커밋 금지**(`.gitignore`에 있음).
  반면 `낭만클럽-세팅/.claude/skills/` 는 교수님 자작이라 커밋한다. 헷갈리지 말 것.
- GitHub Actions에 자동 워크플로우 4개(`daily-threads`, `brunch-draft`, `column-draft`, `claude`)가 돈다. 워크플로우 파일을 고치면 스케줄이 바뀐다 — 고치기 전에 알린다.

---

## 6. 구글 드라이브 작업방 (완성본은 여기로 모인다)

**원칙 한 줄: git은 작업장, 드라이브는 전시장.**
초안·중간 파일은 리포에만 둔다. **완성본만** 드라이브로 올린다. 드라이브를 작업 폴더로 쓰지 않는다(git 리포를 드라이브 동기화 폴더에 두면 `.git`이 깨진다).

### 폴더 (내 드라이브 → `낭만클럽 작업방`)

| 폴더 | 들어가는 것 | 폴더 ID |
|---|---|---|
| `01_원고` | 스레드·칼럼·브런치 완성 원고 | `1QUUuwY3ZLbNezTQg3EcJofwoHCIcIaOd` |
| `02_카드뉴스-이미지` | 카드뉴스 PNG, 렌더 결과, 디자인 파일 | `1fETBLaIh1lqNcwmMFmKf8U8BhhEuT3CU` |
| `03_강의자료` | 슬라이드, 시험문항, 강의 대본, 연간계획표 | `1EyJ6ixCOu-9LYPA1YvmhfIeY_azm7Lhh` |
| `04_전략-제안서` | 사업 제안서, 전략 문서, 기획안 | `1QuJh0U1lukEbMe_ff156Dl2r8YjQ15Dh` |
| `99_보관` | 지난 것, 버전 백업 | `13tISYUR9ZyfmUchbrkCfYB_SZBGZTfB1` |

작업방 루트 ID: `1snTlhPEk76pjX0BoZK00nJdDTwjZnXPM`

### 누가 올리나

- **Claude Code**: 구글 드라이브가 연결돼 있으니 **직접 업로드한다.** 완성본이 나오면 위 표의 폴더 ID로 올리고, 교수님께 링크를 드린다.
- **Codex**: 드라이브에 직접 못 붙는다(별도 MCP 설정 필요, **[미검증]**). 그래서 **리포에 저장하고 push까지만** 한다. 그리고 `.agents/작업일지.md`의 `## 다음 사람에게 넘기는 일`에 `- [ ] (claude) <경로> 드라이브 01_원고에 올려주기` 를 남긴다.

### 파일명 규칙 (예외 없음)

```
YYYY-MM-DD_종류_제목
```

- `2026-09-13_스레드_복날은-닭이-아니었다`
- `2026-09-13_카드뉴스_다이어트트렌드_v3`
- `2026-09-13_제안서_B2B-식자재-공동구매몰`

날짜는 **한국 시간(KST)** 기준이다. 러너·서버 시계는 UTC라 하루 이를 수 있으니 확인하고 쓴다.

### 덮어쓰기 금지

같은 제목이 이미 있으면 덮지 않는다. 뒤에 `_v2`, `_v3`을 붙이고, 이전 버전은 `99_보관`으로 옮긴다. 교수님이 "덮어써라" 하시면 그때만 덮는다.
